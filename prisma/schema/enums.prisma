enum UserRole {
  ADMIN
  USER
}

enum UserStatus {
  ACTIVE
  INACTIVE
  BLOCKED
  DELETED
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum PaymentStatus {
  PAID
  UNPAID
}

enum SubscriptionStatus {
  ACTIVE
  EXPIRED
  CANCELLED
}

enum SubscriptionPlan {
  FREE
  MONTHLY
  YEARLY
}

enum TravelType {
  SOLO
  FAMILY
  FRIENDS
  COUPLE
  GROUP
}

enum TravelPlanStatus {
  OPEN
  CLOSED
  COMPLETED
  CANCELLED
}

enum TravelRequestStatus {
  PENDING
  APPROVED
  REJECTED
}
